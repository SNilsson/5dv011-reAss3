#pragma once


struct cell;
struct termite;
struct grid;
struct simulation;

/**
 * \brief Represents a direction.
 *
 * A direction is one of the four cardinal directions (north, south,
 * west, east).
 */
enum direction { NORTH = 0, EAST = 1, SOUTH = 2, WEST = 3 };


